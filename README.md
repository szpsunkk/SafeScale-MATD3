# SafeScale-MATD3 Initial Implementation

This folder provides an initial, runnable implementation aligned with the paper:

- Unified tick-level environment (`UnifiedEnvironment`)
- Initial SafeScale-MATD3 policy (safety queue + proactive handover + adaptive power)
- Baselines: MA-TD3 and AMDT
- One-click experiments and IEEE-style figure generation

Generated figures:

- `fig_convergence`
- `fig_safety_violation`
- `fig_aoi_by_priority`
- `fig_spike_validation`

All outputs are saved under `outputs/`.

